@model IEnumerable<wardrobe.Models.Outfit>

@{
    ViewBag.Title = "Index";
}

<h2>Outfits</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<div class="row">
    <!--Takes each review and puts it in a thumbnail, 3 thumbnails across medium pages-->
    @foreach (var item in Model)
    {
        <div class="col-md-4">
            <div class="thumbnail">
                <div class="caption">
                    <p>@Html.ActionLink("Details", "Details", new { id = item.OutfitID }, new { @class = "btn btn-primary", role = "button" })</p>
                    
                </div>
            </div>
        </div>
    }
</div>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Accessory.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Bottom.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Shoe.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Top.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Accessory.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Bottom.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Shoe.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Top.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OutfitID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OutfitID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OutfitID })
        </td>
    </tr>
}

</table>
